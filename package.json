{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "NUXT_APP_BASE_URL=/factorio-quality/ npm run generate && gh-pages -d .output/public -t true"
  },
  "dependencies": {
    "gh-pages": "^6.2.0",
    "modern-normalize": "^3.0.1",
    "nuxt": "^3.14.1592",
    "sass": "^1.81.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@types/gh-pages": "^6.1.0"
  }
}
